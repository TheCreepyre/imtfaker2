name: Build Process Killer EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller psutil pywin32

      - name: Build EXE
        run: |
          pyinstaller --onefile --windowed --name ProcessKiller kill_processes.py

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ProcessKiller
          path: dist/ProcessKiller.exe

      - name: Upload lock file template
        uses: actions/upload-artifact@v4
        with:
          name: LockFileTemplate
          path: |
            process_killer.lock
            !dist/
